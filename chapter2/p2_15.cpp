//(左值)引用必须被初始化，初始值不能为常值，绑定后无法换绑，不能定义引用的引用, 不能定义指向引用的指针，但是可以定义指针的别名(引用)
/*
* p2_15
* b 不能初始化为常值， d 未初始化

* p2_16
* 都合法 (a)d = 3.14... (b) d = 0 (c) i = 0 (d)i = d

* p2_17 
* 10 10
*/

// 空指针:nullptr = 0 = NULL ,但是不能通过赋值整型变量0来得到空指针
#include <iostream>
int i = 42;
int main(){
    int i = 0, &r1 = i;
    double d = 0, &r2 = d;
    r2 = 3.14;
    r2 = r1;
    i = r2;
    r1 = d;
    return 0;
}